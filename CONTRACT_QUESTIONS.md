1. Does GET /users require authentication? - Since some users may be anonymous incident reporters, returning all users publicly seems like an invasion of privacy.

2. is it possible for GET /incidents to support filtering by county or status? - The schema shows both fields exist but no query parameters are documented for the endpoint

3. Is there a way to filter GET /volunteers by status? - how does a volunteer's status get updated from pending to approved?



